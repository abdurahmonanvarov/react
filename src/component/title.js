

const Title = (props) =>{
    return (
        <div>
            <h1 className="soloinfo">{props.title}</h1>
            <p className="soloinfo">{props.subtitle}</p>
        </div>
    )
}
export default Title